# cog.toml
tag_prefix = "v"
ignore_merge_commits = true
skip_ci = "[skip ci]"

[commit_types]
feat = { version_update = "minor", changelog_title = "🚀 Features" }
fix = { version_update = "patch", changelog_title = "🐛 Bug Fixes" }
refactor = { changelog_title = "🛠️ Refactors" }
perf = { version_update = "patch", changelog_title = "⚡ Performance" }
docs = { changelog_title = "📖 Documentation" }
chore = { changelog_title = "⚙️ Maintenance" }

[changelog]
path = "CHANGELOG.md"
authors = []

[packages.xolo-pipeline]
path = "."
# Los hooks de abajo actualizan la versión en tus archivos .toml automáticamente
pre_bump_hooks = [
    "sed -i 's/^version = \".*\"/version = \"{{version}}\"/' pyproject.toml",
    "sed -i 's/^version = \".*\"/version = \"{{version}}\"/' Cargo.toml"
]
